# Handoff: Complete runtime Guidance v0

- Date (UTC): 2026-08-27T21:16:57Z
- Branch: `codex/chat-01a02c2ef1c9`
- Worktree: `/Users/benkind/.codex/worktrees/1ebc/Current Flow Main`
- Starting HEAD: `b8e708315414bd38bd5254ab52cbc71f60c72407`
- State: uncommitted, unmerged, and unpublished

## Objective

Complete the six identified Guidance v0 gaps so every valid canonical temporal snapshot produces
the current OLTR format, exactly three Intentions, and exactly three ranked Execution work domains.
Replace atomic task nudges with a five-domain Five Phase taxonomy and include the active Organ.

## Implemented

1. Expanded the Current semantic registry to one `spec-reviewed` product profile for each canonical
   King Wen hexagram 1–64. Registry validation now requires exactly that complete coverage and does
   not invent a human reviewer.
2. Connected the real Home environment. One authoritative `GlobalConditionsSnapshot` per sampling
   cycle supplies only reviewed categorical Cantong and seasonal signals; the same instant's
   local-civil Organ clock supplies the active Organ and Phase. Raw celestial measurements never
   enter guidance.
3. Tightened deterministic OLTR rendering around the existing semicolon grammar. Texture and lunar
   specialization now require exact compatibility; Macro maturity remains subordinate and cannot
   change response relation or effort.
4. Closed Intention selection over the controlled 15-entry lexicon. Every available synthesis gets
   exactly three distinct relation-compatible ranks. Selecting another Intention preserves the
   temporal synthesis, Primary Current, OLTR, evidence, versions, and validity, and re-ranks only
   Execution.
5. Replaced atomic Execution actions with five Current-formalized adult work domains: Wood
   Direction & Development, Fire Communication & Leadership, Earth Operations & Stewardship, Metal
   Refinement & Closure, and Water Insight & Resolve. The engine scores all five, returns the top
   three, and guarantees active-Organ Phase coverage.
6. Completed containment and presentation: typed fail-closed construction errors, complete
   version/coverage/conflict propagation, strict cross-output validation, exact cache identities and
   boundaries, selected/live cache isolation, paired-Fu versus Zang-spirit labeling, responsive
   cards, visible focus, and polite selection announcements.

## Runtime architecture

This is an engine, not a state database. Durable data is limited to 64 profiles, 15 intentions,
five Execution definitions, and deterministic composition rules. For a requested instant, the
provider resolves only the current Year, Month, Day, Hour, Macro, Organ, and eligible environment
signals; the engine composes and validates that one bundle. It does not materialize the nominal
60⁴ (12,960,000-row) four-pillar cross-product or an environment-enriched cross-product.

The closure test generates and immediately discards 64 Day × 64 Hour × two Macro combinations
(8,192 bundles). It is verification of the public runtime path, not a stored corpus.

## Verification

- Focused Guidance, resolver, environment, provider, and panel suites: 5 files, 100 tests passed.
- Generated runtime closure: 8,192/8,192 bundles passed OLTR, three-Intention, three-domain, and
  active-Organ invariants.
- Browser QA: desktop and 390 × 844 mobile layouts inspected; no horizontal overflow; three cards in
  each section; hidden duplicate heading confirmed; Intention selection updated `aria-pressed` and
  the live announcement while preserving OLTR; browser console contained no warnings or errors.
- `npm run check` passed through the bundled workspace runtime: toolchain synchronization, strict
  type-check, zero-warning lint, 53 unit-test files / 446 tests, 11 workspace tests, six gateway
  tests, 64 commentary bundles / 379 summaries / five explicit unavailable records, 64 transition
  bundles / 384 summaries, and the production build.
- `git diff --check` passed.

The production build retained Vite's informational large-chunk warning for the Astrology route; no
verification step failed.

## Decision and documentation

- Added
  `docs/continuity/decisions/20260827T201839Z--complete-runtime-guidance-and-elemental-execution.md`.
- Updated active Guidance, semantic-resolver, architecture, calculation-source, and integration
  documents to match the runtime system.
- Updated the two superseded 2026-08-22 decisions without changing their still-accepted semantic
  gate, source-separation, provenance, review-honesty, or safety boundaries.

## Remaining integration work

- `docs/continuity/PROJECT_STATE.md` still contains the superseded 13-profile/atomic-Execution
  summary. This feature task did not edit it because shared project-state reconciliation is reserved
  for an explicitly authorized integration task.
- The 64 operational profiles are product-specification reviewed, not human-editor approved. A
  future editorial pass may revise them without changing the runtime architecture.
- No commit, push, merge, deployment, or publication was performed in this task.
